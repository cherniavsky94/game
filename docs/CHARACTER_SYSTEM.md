
````markdown
# 🎭 Система персонажей

## Что реализовано

✅ **8 классов персонажей**  
✅ **Создание персонажа с выбором класса**  
✅ **Выбор персонажа из списка**  
✅ **Лимит 10 персонажей на аккаунт**  
✅ **Сохранение в базу данных через Prisma**  
✅ **API endpoints для работы с персонажами**  

## Классы персонажей

### 1. Ассасин (ASSASSIN)
- **Описание**: Мастер скрытности и быстрых атак
- **Базовое HP**: 80
- **Базовая MP**: 60
- **Цвет**: Темно-красный

### 2. Амазонка (AMAZON)
- **Описание**: Искусная лучница с высокой ловкостью
- **Базовое HP**: 90
- **Базовая MP**: 50
- **Цвет**: Зеленый

### 3. Варвар (BARBARIAN)
- **Описание**: Могучий воин ближнего боя
- **Базовое HP**: 150
- **Базовая MP**: 30
- **Цвет**: Золотой

### 4. Страж (GUARDIAN)
- **Описание**: Защитник с высокой броней
- **Базовое HP**: 140
- **Базовая MP**: 40
- **Цвет**: Синий

### 5. Ведьма (WITCH)
- **Описание**: Темная магия и проклятия
- **Базовое HP**: 70
- **Базовая MP**: 120
- **Цвет**: Фиолетовый

### 6. Чародейка (SORCERESS)
- **Описание**: Мастер стихийной магии
- **Базовое HP**: 60
- **Базовая MP**: 140
- **Цвет**: Розовый

### 7. Маг (MAGE)
- **Описание**: Владеет мощной боевой магией
- **Базовое HP**: 65
- **Базовая MP**: 130
- **Цвет**: Королевский синий

### 8. Дриада (DRUID)
- **Описание**: Повелитель природы и превращений
- **Базовое HP**: 100
- **Базовая MP**: 100
- **Цвет**: Лаймовый

## Поток работы

### 1. После входа в систему

Пользователь попадает на экран выбора персонажа:
- Если персонажей нет → автоматически открывается создание персонажа
- Если есть персонажи → показывается список для выбора

### 2. Создание персонажа

1. Выбрать класс из 8 доступных (карточки с описанием)
2. Ввести имя (3-16 символов)
3. Нажать "Создать персонажа"
4. Персонаж сохраняется в базу данных
5. Возврат к экрану выбора персонажа

### 3. Выбор персонажа

1. Просмотр всех созданных персонажей (карточки)
2. Каждая карточка показывает:
	 - Имя персонажа
	 - Класс
	 - Уровень
	 - HP/MP
	 - Кнопка "Играть"
3. Нажать "Играть" для входа в игру
4. Кнопка "+ Создать нового персонажа" (если < 10)

### 4. В игре

- Отображается информация о выбранном персонаже
- Кнопка "← Выбор персонажа" для возврата

## Архитектура

### База данных (Prisma)

```prisma
model Character {
	id         String        @id @default(uuid())
	userId     String
	name       String
	class      CharacterClass
	level      Int           @default(1)
	experience Int           @default(0)
	health     Int
	maxHealth  Int           @default(100)
	mana       Int           @default(50)
	maxMana    Int           @default(50)
	x          Float         @default(0)
	y          Float         @default(0)
	createdAt  DateTime      @default(now())
	updatedAt  DateTime      @updatedAt
}

enum CharacterClass {
	ASSASSIN
	AMAZON
	BARBARIAN
	GUARDIAN
	WITCH
	SORCERESS
	MAGE
	DRUID
}
```

### API Endpoints

**GET /api/characters**
- Получить все персонажи пользователя
- Требует авторизацию (Bearer token)

**POST /api/characters**
- Создать нового персонажа
- Body: `{ name: string, class: CharacterClass }`
- Требует авторизацию

**GET /api/characters/:id**
- Получить конкретного персонажа
- Требует авторизацию

### Клиентские сцены

**CharacterSelectionScene**
- Отображает список персонажей
- Кнопка создания нового
- Кнопка выхода из аккаунта

**CharacterCreationScene**
- Сетка из 8 классов (4x2)
- Поле ввода имени
- Валидация и создание

**GameScene**
- Отображает выбранного персонажа
- Кнопка возврата к выбору

### Сервисы

**CharacterService (Client)**
```typescript
const service = CharacterService.getInstance();

// Получить персонажей
const characters = await service.getCharacters();

// Создать персонажа
const character = await service.createCharacter({
	name: 'Герой',
	class: CharacterClass.BARBARIAN
});
```

**characterService (Server)**
```typescript
// Получить персонажей пользователя
await characterService.getCharactersByUserId(userId);

// Создать персонажа
await characterService.createCharacter(userId, name, class);

// Проверка лимита (max 10)
// Проверка уникальности имени
```

## Валидация

### Имя персонажа
- Минимум 3 символа
- Максимум 16 символов
- Уникально для каждого пользователя

### Лимит персонажей
- Максимум 10 персонажей на аккаунт
- Проверка на сервере при создании

### Класс
- Должен быть один из 8 доступных
- Enum валидация на сервере

## Тестирование

### 1. Создание персонажа

```bash
# Откройте порт 3000
# Войдите в систему
# Выберите класс
# Введите имя
# Нажмите "Создать персонажа"
```

### 2. Проверка в базе данных

```bash
npm run prisma:studio
# Откройте таблицу Character
# Проверьте созданных персонажей
```

### 3. API тестирование

```bash
# Получить токен из браузера (DevTools > Application > Local Storage)
TOKEN="your_token_here"

# Получить персонажей
curl -H "Authorization: Bearer $TOKEN" http://localhost:2567/api/characters

# Создать персонажа
curl -X POST \
	-H "Authorization: Bearer $TOKEN" \
	-H "Content-Type: application/json" \
	-d '{"name":"Тестовый","class":"BARBARIAN"}' \
	http://localhost:2567/api/characters
```

## Следующие шаги

1. ✅ Добавить спрайты для классов
2. ✅ Реализовать удаление персонажей
3. ✅ Добавить редактирование персонажей
4. ✅ Реализовать систему прокачки
5. ✅ Добавить инвентарь персонажа
6. ✅ Интегрировать с игровым миром

## Troubleshooting

### Персонажи не загружаются

1. Проверьте, что сервер запущен (порт 2567)
2. Проверьте консоль браузера на ошибки
3. Проверьте, что Prisma Client сгенерирован:
	 ```bash
	 npm run prisma:generate
	 ```

### Ошибка при создании персонажа

1. Проверьте длину имени (3-16 символов)
2. Проверьте, что выбран класс
3. Проверьте лимит (не более 10 персонажей)
4. Проверьте уникальность имени

### База данных не синхронизирована

```bash
npm run prisma:migrate
```

---

**Готово!** Система персонажей полностью работает. 🎭

````
