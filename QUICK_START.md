# 🚀 Quick Start

## Запуск игры

```bash
./start.sh
```

Или:

```bash
npm run dev
```

## Проверка статуса

```bash
# Проверить порты
lsof -i :3000 -i :2567

# Проверить health сервера
curl http://localhost:2567/health

# Посмотреть логи
tail -f /tmp/client.log
tail -f /tmp/server.log
```

## Остановка

```bash
pkill -f "vite --host"
pkill -f "tsx watch"
```

## Доступ к игре

1. Откройте панель **PORTS** в Gitpod/VS Code
2. Найдите порт **3000** (клиент) - нажмите 🌐
3. Найдите порт **2567** (сервер) - нажмите 🌐

## Порты

- **3000** - Phaser 4 клиент
- **2567** - Colyseus сервер
- **2567/colyseus** - Панель мониторинга
- **2567/health** - Health check

## Troubleshooting

### Порт занят

```bash
lsof -ti:3000 | xargs kill -9
lsof -ti:2567 | xargs kill -9
```

### Перезапуск

```bash
./start.sh
```

### Логи показывают ошибки

```bash
# Переустановить зависимости
npm install

# Пересобрать shared
npm run build --workspace=shared

# Перезапустить
./start.sh
```

## Что должно работать

✅ Клиент показывает изометрическую сетку  
✅ Сервер отвечает на /health  
✅ Стрелки на клавиатуре отправляют команды  
✅ Консоль браузера показывает "🎮 Isometric RPG Client Started"  
✅ Можно открыть несколько вкладок для тестирования мультиплеера  

## Следующие шаги

1. Откройте порт 3000 в браузере
2. Откройте консоль браузера (F12)
3. Нажимайте стрелки на клавиатуре
4. Откройте вторую вкладку для тестирования мультиплеера
5. Откройте /colyseus на порту 2567 для мониторинга

Готово! 🎮
